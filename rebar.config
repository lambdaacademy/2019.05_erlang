{erl_opts, [debug_info]}.
{deps, []}.

{shell, [
  % {config, [{config, "config/sys.config"}]},
    {apps, [conway_game]}
]}.

{relx, [{release, {conway_game, "0.0.1"},
         [conway_game]},

        {dev_mode, true},
        {include_erts, false},

        {extended_start_script, true}]}.
